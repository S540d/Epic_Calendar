#!/bin/sh

echo "🔍 Running Prettier format check..."
if ! npx prettier --check "**/*.{ts,tsx}" --ignore-path .gitignore; then
  echo "❌ Prettier format check failed. Run: npm run format"
  exit 1
fi
echo "✅ pre-push checks passed"
